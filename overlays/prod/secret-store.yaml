apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: incident-kb
      auth:
        workloadIdentity:
          clusterLocation: us-central1
          clusterName: polaris-prod
          clusterProjectID: incident-kb
          serviceAccountRef:
            name: relynce
