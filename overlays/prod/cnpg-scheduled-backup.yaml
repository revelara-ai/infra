apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: relynce-db-backup
  namespace: prod
spec:
  schedule: "0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: relynce-db
  immediate: true
